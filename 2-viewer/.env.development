# Development environment configuration
# Used when running locally with `npm run dev`

# Local Caddy server (if running)
# VITE_CADDY_HOST=127.0.0.1
# VITE_CADDY_PORT=3002

# Environment mode
VITE_ENVIRONMENT=development

# PMTiles source for development (local files)
# VITE_PMTILES_SOURCE=local
VITE_PMTILES_SOURCE=cloudflare
VITE_CLOUDFLARE_WORKER_URL=https://pmtiles-cloudflare.mheaton-945.workers.dev

# Options: 
# - local: Use local PMTiles files from /public/tiles or /tiles
# - caddy: Use self-hosted Caddy server (requires running docker-compose)
# - cloudflare: Use Cloudflare Workers (for testing production setup locally)
