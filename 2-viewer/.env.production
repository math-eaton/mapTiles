# Production environment configuration
# Used when building for deployment with `npm run build`

# Environment mode
VITE_ENVIRONMENT=production

# PMTiles source for production (Cloudflare Workers + R2)
VITE_PMTILES_SOURCE=cloudflare

# Cloudflare Worker URL (update this to your actual worker URL)
# Format: https://your-worker-name.your-subdomain.workers.dev
# or your custom domain if configured
VITE_CLOUDFLARE_WORKER_URL=https://pmtiles-cloudflare.mheaton-945.workers.dev

# For Cloudflare Pages deployment, you can also use:
# VITE_CLOUDFLARE_WORKER_URL=/api/tiles
# (if you set up a Pages function or route to your worker)
