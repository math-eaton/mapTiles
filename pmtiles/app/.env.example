# =============================================================================
# PMTiles Configuration for Cloudflare Workers + R2
# =============================================================================
# This file documents the environment variables used for serving PMTiles
# archives via Cloudflare Workers with R2 storage.
#
# SETUP:
# 1. Copy this file to `.env` (git-ignored)
# 2. Update VITE_CLOUDFLARE_WORKER_URL with your worker URL
# 3. Optionally customize archive names if different from defaults
# =============================================================================

# REQUIRED: Your Cloudflare Worker URL for PMTiles delivery
# The worker should be deployed with R2 bucket binding named "BUCKET"
# pointing to your grid3-maptiles R2 bucket
VITE_CLOUDFLARE_WORKER_URL=https://pmtiles-cloudflare.mheaton-945.workers.dev

# =============================================================================
# PMTiles Archive Names (Optional - defaults provided)
# =============================================================================
# These correspond to .pmtiles files in your R2 bucket
# The worker serves: {WORKER_URL}/{ARCHIVE_NAME}/{z}/{x}/{y}.mvt

# Protomaps basemap (OpenStreetMap-based layers)
# File in R2: global.pmtiles
VITE_PROTOMAPS_ARCHIVE=global

# Overture buildings data layer
# File in R2: buildings.pmtiles
VITE_OVERTURE_ARCHIVE=buildings

# GRID3 custom data layer
# File in R2: grid3.pmtiles
VITE_GRID3_ARCHIVE=grid3

# =============================================================================
# Asset Configuration (Optional - defaults to Protomaps CDN)
# =============================================================================
# If you host your own sprites and fonts in R2, update these:

# Sprite base URL (without the theme suffix)
# Default uses Protomaps CDN sprites
# VITE_SPRITE_BASE_URL=https://protomaps.github.io/basemaps-assets/sprites/v4

# Glyphs URL pattern for fonts
# VITE_GLYPHS_URL=https://protomaps.github.io/basemaps-assets/fonts/{fontstack}/{range}.pbf

# =============================================================================
# R2 Bucket Structure
# =============================================================================
# Your R2 bucket should contain:
#   grid3-maptiles/
#   ├── global.pmtiles    (Protomaps basemap, ~100GB)
#   ├── buildings.pmtiles (Overture buildings)
#   └── grid3.pmtiles     (GRID3 data layer)
#
# Optional assets (if self-hosting):
#   ├── sprites/
#   │   └── v4/
#   │       ├── light.json
#   │       ├── light@2x.json
#   │       └── ...
#   └── fonts/
#       └── Noto Sans Regular/
#           └── *.pbf
# =============================================================================
