# Example Environment Configuration
# Copy this to .env.development and .env.production and update with your values

# ============================================
# Development Configuration (.env.development)
# ============================================

# Environment mode
VITE_ENVIRONMENT=development

# PMTiles source for development
# Options: local, caddy, cloudflare, auto
VITE_PMTILES_SOURCE=local

# Local Caddy server (if using)
VITE_CADDY_HOST=127.0.0.1
VITE_CADDY_PORT=3002

# Cloudflare Worker URL (for testing production setup locally)
# VITE_CLOUDFLARE_WORKER_URL=https://pmtiles-cloudflare.your-subdomain.workers.dev


# ============================================
# Production Configuration (.env.production)
# ============================================

# Environment mode
# VITE_ENVIRONMENT=production

# PMTiles source for production
# VITE_PMTILES_SOURCE=cloudflare

# Cloudflare Worker URL (REQUIRED for production)
# Update this to your actual Worker URL after deployment
# VITE_CLOUDFLARE_WORKER_URL=https://pmtiles-cloudflare.your-subdomain.workers.dev

# Or use custom domain:
# VITE_CLOUDFLARE_WORKER_URL=https://tiles.yourdomain.com


# ============================================
# PMTiles Source Options
# ============================================

# local: Use local PMTiles files from /tiles directory
#        Good for: Local development, testing
#        Requires: PMTiles files in public/tiles/ or accessible path

# caddy: Use self-hosted Caddy server
#        Good for: Development with docker-compose
#        Requires: Caddy server running (see 3-server/)

# cloudflare: Use Cloudflare Worker + R2
#        Good for: Production, testing production setup
#        Requires: Worker deployed, R2 bucket configured

# auto: Automatically detect and use best available source
#        Priority: cloudflare > caddy > local
#        Good for: Seamless dev/production workflow
